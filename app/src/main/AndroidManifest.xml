<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" 
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:requestLegacyExternalStorage="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.WarehouseCamera">
        
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".ui.ManufacturerInfoActivity" />
        <activity android:name=".ui.ArticleInfoActivity" />
        <activity android:name=".ui.DefectDetailsActivity" />
        <activity android:name=".ui.CameraActivity" />
        <activity android:name=".ui.ItemListActivity" />
        <activity android:name=".ui.GalleryActivity" />
        <activity android:name=".ui.ImageViewerActivity" />
        <activity android:name=".ui.gallery.GalleryBrowserActivity" />
        <activity android:name=".ui.reception.ReceptionSelectionActivity" />
        <activity android:name=".ui.reception.CreateReceptionActivity" />
        <activity android:name=".ui.help.HelpActivity" />
        <activity android:name=".ui.FileStructureActivity" />
        <activity android:name=".ui.scanner.BarcodeScannerActivity" />
        
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>